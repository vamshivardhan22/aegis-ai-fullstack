# Aegis AI Frontend

React 18 + Vite + Tailwind CSS frontend for Aegis AI v2.0.

## Run

```bash
cd aegis-frontend
npm install
npm run dev
```

The app runs on `http://localhost:5173` and expects the FastAPI backend at `http://localhost:8000`.

## Build

```bash
npm run build
```
